    // temp.opcode = "LB";
    // temp.rd = stoi(tokens[0].substr(1));
    // size_t pos = tokens[1].find('(');
    // size_t pos2= tokens[1].find(')');
    // temp.immidiate = stoi(tokens[1].substr(0,pos));
    // temp.r1 = stoi(tokens[1].substr(pos+1,pos2));
    // program.push_back(temp);